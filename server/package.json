{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-client": " cd ../client && npm install && npm run build",
    "start": "nodemon index.js & npm run client",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run build-client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "ytdl-core": "^3.1.1"
  }
}
